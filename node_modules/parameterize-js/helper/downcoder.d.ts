import { Keymap } from "../interfaces/keymap.interface";
export declare class Downcoder {
    private keymap;
    private chars;
    private regex;
    constructor(...keymaps: Keymap[]);
    apply(slug: string): string;
}
export declare const DEFAULT_DOWNCODER: Downcoder;
